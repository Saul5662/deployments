---
# Install Grafana Alloy from the official Grafana APT/RPM repository.
# Uses the modern signed-by approach (no apt-key).

- name: Install Grafana Alloy using {{ target_pkg_module.split('.')[-1] }}
  ansible.builtin.include_tasks: "install_{{ target_pkg_module.split('.')[-1] }}.yml"