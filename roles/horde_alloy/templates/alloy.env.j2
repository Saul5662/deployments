# {{ ansible_managed }}
# Alloy systemd environment — sourced by the alloy.service unit.
# See: systemctl cat alloy

CUSTOM_ARGS="--server.http.listen-addr={{ horde_alloy_http_listen_address }}:{{ horde_alloy_http_port }} --stability.level=generally-available"
CONFIG_FILE={{ horde_alloy_config_dir }}/config.alloy
